# Base URL of the AccountabiliBuddy relayer (the oracle/crank HTTP service).
# Defaults to the shared production relayer if unset. Copy to `.env.local` to override.
VITE_RELAYER_URL=https://66.42.115.38
