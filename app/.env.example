# Optional relayer override. If unset:
# - `npm run dev` uses http://localhost:8787
# - production uses the web app's current HTTPS origin
#
# Copy this file to `.env.local` only when you need an explicit override.
# VITE_RELAYER_URL=https://66.42.115.38.nip.io
